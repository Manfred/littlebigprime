secret:
  digest: <%= Password.new(plain_text: 'secret').digest %>
  prefix: sec
leaked:
  digest: <%= Password.new(plain_text: 'leaked').digest %>
  prefix: lea
deactivated:
  digest: <%= Password.new(plain_text: 'deactivate').digest %>
  prefix: dea
  deactivated_at: <%= 1.day.ago %>
